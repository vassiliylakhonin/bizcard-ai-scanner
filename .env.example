# Frontend (dev/local-only mode). Vite only exposes env vars prefixed with VITE_.
# If you plan to deploy publicly, do NOT ship an API key to the browser.
VITE_GEMINI_API_KEY=
VITE_OPENAI_API_KEY=
VITE_ANTHROPIC_API_KEY=
VITE_OPENAI_COMPAT_API_KEY=
VITE_OPENAI_COMPAT_BASE_URL=

# Optional provider model/base overrides
VITE_GEMINI_MODEL=
VITE_OPENAI_MODEL=
VITE_OPENAI_BASE_URL=
VITE_ANTHROPIC_MODEL=
VITE_ANTHROPIC_BASE_URL=
VITE_OPENAI_COMPAT_MODEL=

# Frontend (recommended production mode): call a backend proxy instead of exposing keys in-browser.
# In dev, you can use the included server on :8787 and the Vite dev server will proxy /api/* to it.
# Note: included backend proxy currently supports Gemini extraction.
VITE_USE_BACKEND=false
VITE_BACKEND_URL=

# Backend proxy (server/index.js)
GEMINI_API_KEY=
PORT=8787
